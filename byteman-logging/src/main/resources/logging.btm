RULE hello entry
CLASS App
METHOD hello
HELPER org.example.byteman.logging.Logger
AT ENTRY
BIND count = $1;
IF true
DO log(Log$Context.HELLO_METHOD_ENTRY, count)
ENDRULE

RULE hello exit
CLASS App
METHOD hello
HELPER org.example.byteman.logging.Logger
AT EXIT
BIND count = $1;
IF true
DO log(Log$Context.HELLO_METHOD_EXIT, count)
ENDRULE

RULE app exit
CLASS org.example.byteman.logging.Main
METHOD main
HELPER org.example.byteman.logging.Logger
AT EXIT
IF true
DO close()
ENDRULE
